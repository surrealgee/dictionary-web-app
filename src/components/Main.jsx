import { useState } from "react";
import styled from "styled-components";
import SearchBar from "./SearchBar";

function Main() {
  return (
    <main>
      <SearchBar />
    </main>
  );
}

export default Main;
